{
  "name": "@cf-session-hub/cli",
  "version": "0.1.0",
  "private": true,
  "description": "cfhub: terminal client for CF Session Hub",
  "type": "module",
  "bin": {
    "cfhub": "./dist/src/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@cf-session-hub/core": "0.1.0",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
